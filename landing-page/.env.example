# URL of the Yaadein agent server (app/server.js).
# Local dev: leave unset → defaults to http://localhost:3000
# Deployment: set to your hosted agent server URL, e.g.
# VITE_API_BASE=https://yaadein-agent.up.railway.app
VITE_API_BASE=
