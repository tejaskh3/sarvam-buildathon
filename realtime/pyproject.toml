[project]
name = "yaadein-realtime"
version = "0.1.0"
description = "Pipecat WebRTC voice transport for Yaadein"
requires-python = ">=3.12,<3.14"
dependencies = [
  "pipecat-ai[runner,sarvam,silero,webrtc]==1.6.0",
]

[tool.uv]
package = false
