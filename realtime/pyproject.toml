[project]
name = "yaadein-realtime"
version = "0.1.0"
description = "Pipecat WebSocket voice transport for Yaadein"
requires-python = ">=3.12,<3.14"
dependencies = [
  "pipecat-ai[runner,sarvam,silero,websocket]==1.6.0",
]

[tool.uv]
package = false
